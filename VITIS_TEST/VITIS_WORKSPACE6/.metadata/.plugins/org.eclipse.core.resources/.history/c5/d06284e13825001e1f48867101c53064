#ifndef XPS_BOARD_ZCU111
#define XPS_BOARD_ZCU111
#endif

#ifndef __BAREMETAL__
#define __BAREMETAL__
#endif

int inst_process(struct tcp_pcb *tpcb, char * TCP_data);
unsigned long long int read_sampling_freq(struct tcp_pcb *tpcb);
char * int642str(u64 val, char * str_dest);
char * substring(char * str_dest,char * str,u64 start,u64 end);
unsigned long long int string_count(char* str, u64 pos, char spc);
unsigned long long int string2int64(char* str);

