#ifndef XPS_BOARD_ZCU111
#define XPS_BOARD_ZCU111
#endif

#ifndef __BAREMETAL__
#define __BAREMETAL__
#endif

u64 inst_process(struct tcp_pcb *tpcb, char * TCP_data);
u64 read_sampling_freq(struct tcp_pcb *tpcb);
char * int642str(u64 val, char * str_dest);
char * substring(char * str_dest,char * str,u64 start,u64 end);
u64 string_count(char* str, u64 pos, char spc);
uint_64t string2int64(char* str);

